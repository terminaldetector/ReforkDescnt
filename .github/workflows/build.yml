name: Build DRMD 6DOF

on:
  push:
    branches: [ '**' ]
  pull_request:
  workflow_dispatch:

jobs:
  fabric-pc:
    name: Fabric PC jar
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Fabric mod
        run: |
          # Clear previously-committed mod jars BEFORE the build, not after. The version is part of the
          # filename, so without this a version bump leaves the old jar sitting in dist/ beside the new
          # one under a name that still looks current -- the same "crashing build under a plausible
          # name" hazard the commit step below exists to prevent, reached from the other direction.
          # It has to happen before `build` because packageTlauncher (build.finalizedBy) writes
          # dist/drmd-6dof-<version>-fabric-1.21.1.jar itself: pruning afterwards deletes the very jar
          # INSTALL_TLAUNCHER.txt tells players to download.
          rm -f dist/drmd-6dof-*.jar
          ./gradlew build --no-daemon --stacktrace

      - name: Stage PC jar into dist/
        run: |
          mkdir -p dist
          cp build/libs/drmd-6dof-*.jar dist/
          rm -f dist/*-sources.jar dist/*-dev.jar dist/*-dev-shadow.jar || true
          ls -lh dist/

      - name: Upload Fabric PC mod
        uses: actions/upload-artifact@v4
        with:
          name: drmd-6dof-pc
          path: |
            dist/drmd-6dof-*.jar
            dist/INSTALL_TLAUNCHER.txt
            !dist/*-sources.jar
          if-no-files-found: error
          retention-days: 30

      # dist/ is checked in so the jar can be downloaded straight from the repository — no Actions
      # login, no nested zip. Keeping it current by hand is what left a crashing build sitting there
      # under a name that looked like the latest one, so the build refreshes it itself.
      #
      # A push made with GITHUB_TOKEN does not start another workflow run, so this cannot loop.
      - name: Commit refreshed jar into dist/
        if: github.event_name == 'push'
        continue-on-error: true
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A 'dist/drmd-6dof-*.jar' dist/INSTALL_TLAUNCHER.txt
          if git diff --cached --quiet; then
            echo "dist/ already matches this build"
            exit 0
          fi
          git commit -m "Refresh dist jar from ${GITHUB_SHA:0:7} [skip ci]"
          git push origin HEAD:"${GITHUB_REF_NAME}"
