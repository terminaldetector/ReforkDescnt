#!/usr/bin/env python3
"""Regenerate 16x16 DRMD item icons (Pillow). Run from repo root."""
print("Re-run the generator embedded in git history / agent session if needed.")
print("Textures live in src/main/resources/assets/drmd/textures/item/")
print("Models point to drmd:item/<id>")
