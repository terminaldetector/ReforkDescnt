<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Hold a high-performance Wi-Fi lock during downloads (modem at full power). -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Ask to be exempt from battery optimisation so the OS doesn't kill the
         long background job ("app stopped" on aggressive OEMs). -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Gives the offscreen WebView a real window so it keeps rendering in the
         background (otherwise progress stalls/resets when minimised). -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@android:drawable/ic_menu_save"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.LJtoPDF">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Signing in to Facebook happens here, in a real browser window:
             the credentials go straight from the WebView to Facebook and the
             app keeps only the resulting session cookies. -->
        <activity
            android:name=".FacebookLoginActivity"
            android:exported="false"
            android:label="Facebook"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <service
            android:name=".ConvertService"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
