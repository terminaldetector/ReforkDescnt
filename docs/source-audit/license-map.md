# PHASE 0 · Матрица лицензий

*Проверено чтением файлов LICENSE у upstream-проектов, не по памяти. Форк наследует лицензию
оригинала, поэтому для этого раздела форки не нужны.*

**DRMD распространяется под MIT** (`LICENSE`, `fabric.mod.json`). Всё ниже — следствие этого факта.

Это не юридическая консультация. Это техническое ограничение, из которого следует порядок работ.

## Таблица

Проверено по **самим форкам** (`raw.githubusercontent.com/terminaldetector/<repo>/HEAD/LICENSE`), а
не по upstream: у трёх из них фактическая лицензия оказалась не та, которую предполагает план. Где
это важно, upstream указан отдельно.

| Форк | Лицензия | Можно копировать код в DRMD? |
|---|---|---|
| `BitsAndChisels` | **CC0-1.0** (public domain) | **Да**, вообще без условий |
| `voxel-horizon-java-edition` | MIT | **Да**, с сохранением копирайта |
| `distant-horizons` (форк) | MIT — но это данные, не код | нечего копировать |
| `Valkyrien-Skies` (VS1) | Apache-2.0 | **Да**, с NOTICE и указанием изменений |
| `Eureka` | Apache-2.0 | **Да**, с NOTICE и указанием изменений |
| `Clockwork` | Apache-2.0 | **Да**, с NOTICE и указанием изменений |
| `NoCubes` | LGPL-3.0 | **Нет** — только идея и своя реализация |
| `LittleTiles` | LGPL-3.0 | **Нет** — только идея и своя реализация |
| `bobby` | GPL-3.0 | **Нет** |
| `ImmersiveAircraft` | GPL-3.0 | **Нет** |
| `BlockPhysics` | лицензии нет вообще | **Нет** — по умолчанию все права сохранены |

Проекты из плана, которых среди форков нет, и их upstream-лицензии — на случай, если они появятся:

| Проект | Лицензия upstream | Проверено по |
|---|---|---|
| Create | MIT (код) / All Rights Reserved (ассеты `assets/`) | `Creators-of-Create/Create@master/LICENSE.md` |
| Immersive Portals | GPL-3.0 | `iPortalTeam/ImmersivePortalsMod@master/LICENSE` |
| Distant Horizons (движок) | GPL-3.0 | `distant-horizons-team/distant-horizons@main/LICENSE.txt` (GitLab) |
| Valkyrien Skies **2** | LGPL-3.0 | `ValkyrienSkies/Valkyrien-Skies-2@main/LICENSE` |
| Chisels & Bits | MIT | `ChiselsAndBits/Chisels-and-Bits@master/LICENSE` |
| RingWorld | не установлено | нужно имя репозитория |

## Что из этого следует для плана

### Приоритет доноров задаётся лицензией, и он не совпадает с их весом

Интуиция подсказывает начинать с самых мощных проектов. Лицензии говорят другое, и после проверки
самих форков картина оказалась лучше, чем можно было ожидать:

1. **`BitsAndChisels` — CC0.** Отказ от прав. Копировать можно что угодно, атрибуция не требуется.
   И он делает ровно то, что нужно разделу 8: greedy meshing поверх Fabric Renderer API — тот же
   рендер-стек, что у DRMD.
2. **`voxel-horizon-java-edition` — MIT.** Оказался не модом, а отдельным движком на Java 21 и
   LWJGL, построенным вокруг стриминга и LOD. Занимает место выпавшего Distant Horizons, и на
   лучших условиях: из DH копировать было бы нельзя (GPL-3.0), отсюда можно.
3. **`Valkyrien-Skies`, `Eureka`, `Clockwork` — Apache-2.0.** Форк VS — это первая версия, не
   вторая, и именно поэтому Apache, а не LGPL. Копирование разрешено с NOTICE и указанием
   изменений. Это закрывает разделы 5 и 6 плана — физику и сборку структур.
4. **`NoCubes`, `LittleTiles` (LGPL-3.0), `bobby`, `ImmersiveAircraft` (GPL-3.0)** — читать как
   **описание алгоритма**, реализовывать заново. Раздел 3 плана («не портировать вслепую») здесь
   становится не рекомендацией, а условием лицензии.
5. **`BlockPhysics`** — пропустить: лицензии нет, значит по умолчанию все права сохранены за
   автором.

Для четвёртой группы результат чтения стоит оформлять как текстовое описание алгоритма в
`algorithm-map.md` и реализовывать уже по описанию, а не по исходнику рядом на экране. Разница
между «прочитал и понял» и «переписал построчно» юридически существенна.

### LGPL — отдельный случай

LGPL-3.0 (`NoCubes`, `LittleTiles`) запрещает копирование исходников в MIT-проект, но **разрешает
использование как библиотеки**, если пользователь может подменить эту библиотеку своей версией. Для
модов Minecraft это выполняется естественно: мод ставится отдельным jar, и игрок волен заменить его.

То есть остаётся законный третий путь помимо «скопировать» и «переписать»: **зависимость времени
выполнения**. Он не подходит для ядра D6 (план в разделе 4 прямо запрещает превращать DRMD в набор
адаптеров), но подходит там, где чужой мод остаётся чужим модом.

### Проблема, которая уже есть в репозитории

`libs/immersiveportals-6.0.6-mc1.21.1-fabric.jar` закоммичен в дерево (`b794e6e`), а
`build.gradle` подключает его через `modImplementation files(...)`. `ImmPtlMirrorBridge` импортирует
`qouteall.imm_ptl.core.api.PortalAPI` напрямую.

Immersive Portals — **GPL-3.0**. Из этого следуют два независимых вопроса:

1. **Распространение бинарника.** MIT-репозиторий раздаёт чужой GPL-3.0 jar без текста GPL и без
   предложения исходников. Это вопрос сам по себе, независимо от кода DRMD.
2. **Компиляция против GPL API.** Прямая линковка — сильный аргумент за то, что результат является
   производной работой, а производная работа GPL-3.0 не может распространяться под MIT.

Дешёвое и полное решение — оба пункта закрываются одним изменением: **убрать jar из репозитория и
перевести мост на рефлексию.** `PortalComplexity.hasImmersivePortals()` уже определяет наличие мода
в рантайме, и после работ последних дней нативный путь (`linkNatively`, `PortalTravel`) работает без
ImmPtl вообще — то есть мост перестал быть несущим и стал опциональным ускорителем. Замена
десятка вызовов `PortalAPI` на рефлексию убирает и compile-time зависимость, и бинарник из дерева.

Это, кстати, совпадает с разделом 26 плана: Minecraft — платформа, DRMD — система. Чужой мод не
должен быть ни тем, ни другим.

## Что нужно сделать по разделу 20 плана

Файл `THIRD_PARTY_ATTRIBUTIONS.md` имеет смысл заводить не сейчас, а когда появится первая
скопированная строка: пустая таблица атрибуций хуже отсутствующей, потому что создаёт впечатление,
что вопрос закрыт. Пока в дереве нет ни одной строки чужого кода, кроме упомянутого jar, — и
правильный первый шаг именно по нему.
