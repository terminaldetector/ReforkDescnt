package com.terminaldetector.drmd.world.fate;

/** Narrative fate of the shared DRMD world. */
public enum WorldFate {
	/** Default — machines still run their last orders. */
	CONTINUING,
	/** Ending 1: giga-reactor destroyed — silence, slow decay. */
	SILENCE,
	/** Ending 2: planetary core cracked — absolute void flight. */
	VOID
}
