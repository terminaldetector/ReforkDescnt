org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.configuration-cache=false

minecraft_version=1.21.1
loader_version=0.16.14
loom_version=1.10-SNAPSHOT
yarn_mappings=1.21.1+build.3
fabric_version=0.116.15+1.21.1

# Vendored under libs/ — upstream (iPortalTeam/ImmersivePortalsMod) archived itself 2026-04-21, so
# there is no live release feed to point a remote repository at. This is that archive's own final
# 1.21 branch build, same yarn_mappings as this project. See docs/IMMPTL_STACK.md.
immersive_portals_version=6.0.6

mod_version=1.1.6
maven_group=com.terminaldetector
archives_base_name=drmd-6dof

junit_version=5.11.3
